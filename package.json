{
  "private": true,
  "devDependencies": {
    "husky": "^9",
    "lint-staged": "^15"
  },
  "lint-staged": {
    "*.go": "mise exec -- goimports -w",
    "web/src/**/*.{ts,tsx}": [
      "npx prettier --write",
      "npx eslint --config web/eslint.config.js --fix"
    ]
  },
  "scripts": {
    "prepare": "husky"
  }
}
